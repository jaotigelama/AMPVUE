import { Bundle } from 'bundle-runner';
import { RenderOptions, SSRContext } from './renderer';
declare type BundleRenderOptions = {
    runInNewContext?: boolean | 'once';
    basedir?: string;
    vueServerRenderer: typeof import('@vue/server-renderer');
} & RenderOptions;
export declare function createBundleRenderer(_bundle: Bundle, renderOptions: BundleRenderOptions): {
    renderToString(ssrContext: SSRContext, evalContext: Object): Promise<string>;
};
export {};
